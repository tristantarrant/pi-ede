plugin:
  name: bluesbreaker
  version: main
  source:
    type: git
    url: https://github.com/brummer10/bluesbreaker.lv2.git
  build:
    - make mod
  install:
    - make install INSTALL_DIR=${LV2_DIR}
