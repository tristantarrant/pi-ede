plugin:
  name: shiro
  version: master
  source:
    type: git
    url: https://github.com/tristantarrant/SHIRO-Plugins.git
  build:
    - make NOOPT=true
  install:
    - make install PREFIX=${PREFIX_DIR}
