plugin:
  name: gxtubedistortion
  version: master
  source:
    type: git
    url: https://github.com/brummer10/GxTubeDistortion.lv2.git
  build:
    - make mod
  install:
    - make install INSTALL_DIR=${LV2_DIR}
