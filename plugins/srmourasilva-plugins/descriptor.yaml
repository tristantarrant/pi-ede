plugin:
  name: srmourasilva-plugins
  version: master
  source:
    type: git
    url: https://github.com/SrMouraSilva/plugins-lv2.git
  build:
    - make -C plugins
  install:
    - make -C plugins install PREFIX=${PREFIX_DIR}
  bundles:
    - gossiper.lv2
    - tetr4-switch.lv2
  dependencies: []
