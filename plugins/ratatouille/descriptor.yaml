plugin:
  name: ratatouille
  version: v0.9.10
  source:
    type: git
    url: https://github.com/brummer10/Ratatouille.lv2.git
  build:
    - make modapp
  install:
    - make install INSTALL_DIR=${LV2_DIR}
