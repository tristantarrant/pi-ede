plugin:
  name: harmonicexciter
  version: main
  source:
    type: git
    url: https://github.com/brummer10/HarmonicExciter.git
  build:
    - make mod
  install:
    - make install INSTALL_DIR=${LV2_DIR}