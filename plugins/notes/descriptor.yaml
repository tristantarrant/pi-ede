plugin:
  name: notes
  version: master
  source:
    type: git
    url: https://git.ventosus.ch/notes.lv2
  build:
    - meson setup build -Dlv2dir=${LV2_DIR}
    - meson compile -C build
  install:
    - meson install -C build
