plugin:
  name: x42-mclk
  version: master
  source:
    type: git
    url: https://github.com/x42/mclk.lv2.git
  build:
    - make MOD=1 BUILDOPENGL=no BUILDGTK=no HAVE_UI=no
  install:
    - make install PREFIX=/usr DESTDIR=${TARGET_DIR} MOD=1 BUILDOPENGL=no BUILDGTK=no HAVE_UI=no
  bundles:
    - mclk.lv2
  dependencies: []
