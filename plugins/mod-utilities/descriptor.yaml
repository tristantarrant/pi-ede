plugin:
  name: mod-utilities
  version: master
  source:
    type: git
    url: https://github.com/moddevices/mod-utilities.git
  build:
    - make
  install:
    - make install INSTALL_PATH=${LV2_DIR}
